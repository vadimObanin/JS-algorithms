"use strict"
function bubbleSort(arr) {
    for (let i = 0; i < arr.length - 1; i++) {
        let endSort = arr.length;
        for (let j = 0; j<arr.length - 1; j++ ) {
            if (endSort > 0) {
                
            }
        }
    }
}
